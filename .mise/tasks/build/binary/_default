#!/usr/bin/env bash
set -euo pipefail
#MISE description="Build the Go binary"

: "${GOOS:=$(go env GOOS)}"
: "${GOARCH:=$(go env GOARCH)}"
: "${OUTPUT:=./bin/lucidvault}"

CGO_ENABLED=0 GOOS="${GOOS}" GOARCH="${GOARCH}" go build -trimpath -ldflags="-s -w" -o "${OUTPUT}" ./cmd/main.go
