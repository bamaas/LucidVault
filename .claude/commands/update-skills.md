Update all Claude Code skills by running:

git submodule update --remote --merge .claude/skills/

Then for each submodule that was updated, run `git log ORIG_HEAD..HEAD --oneline` inside it to show what commits came in. Summarize the changes.
