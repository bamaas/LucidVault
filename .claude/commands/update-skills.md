Update all Claude Code skills by running:

git submodule update --remote --merge .claude/cc-skills-golang

Then for each updated submodule, run `git log ORIG_HEAD..HEAD --oneline` inside `.claude/cc-skills-golang` to show what commits came in. Summarize the changes.
