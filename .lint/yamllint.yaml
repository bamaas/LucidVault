---
extends: default

ignore:
  - ".claude/skills/"
  - ".claude/cc-skills-golang/"

rules:
  document-start: disable
  line-length:
    max: 160
  truthy:
    allowed-values: ["true", "false", "on"]
