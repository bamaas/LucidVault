# AGENTS.md -- Vault Instructions for AI Agents

This file is auto-generated by LucidVault. Do not edit manually.

## Vault Access Rules

- **Read directly**: wiki/, raw/, notes/, index.md, soul.md, AGENTS.md
- **Write through MCP only**: Use MCP tools for all mutations (add_bookmark, add_note, update_wiki, delete_page)
- **Never write files directly** -- vault files have strict formatting requirements enforced by the MCP server

## Retrieval Strategy

1. Read `index.md` for topic discovery (slug, title, tags for every page)
2. Search `wiki/` first -- these are LLM-enriched summaries, preferred for most queries
3. Check `notes/` for personal notes if wiki pages don't fully answer
4. Read `raw/` only when wiki summary lacks detail -- raw files can be very large
5. Use `expand_graph` to discover related content clusters
6. Follow `[[wikilinks]]` in wiki pages to explore connections
7. Use `related_notes` for bidirectional link discovery

## Content Guidelines

- Prefer wiki/ pages over raw/ -- wiki pages are concise summaries
- Read first 200 lines of raw/ files, not full content (they can be very large)
- When citing info, reference the wiki page slug
- soul.md describes the vault owner -- read it for personalization context

## Query Expansion

Broaden every query before searching -- a single literal phrasing hurts recall:

- **Synonyms & abbreviations** -- expand short forms and alternates (e.g. "k8s" ->
  "kubernetes", "ml" -> "machine learning"). Run each variant and merge the results.
- **Personalization** -- read `soul.md` for the owner's interests and expertise to
  disambiguate ambiguous terms toward their domain (e.g. "go" -> the language).
- **Lateral terms** -- pull tags and `[[wikilinks]]` from the top hit and re-search
  with them to reach adjacent content the original query missed.

## Source Attribution

Always state where each piece of an answer came from. For blended answers,
attribute each part separately:

- **Vault** -- cite the exact file path or wiki slug (e.g. `wiki/raft-consensus.md`).
- **Model knowledge** -- say so explicitly ("from my own knowledge, not the vault").
- **Web search** -- cite the source URL (and provider).

## Web Search

- Search the vault (`wiki/`, `notes/`, `raw/`) first -- it is the owner's curated
  knowledge and should be preferred.
- Reach for web search only when the vault does not cover the question.
- Always cite retrieved URLs (see Source Attribution) so answers stay verifiable.
